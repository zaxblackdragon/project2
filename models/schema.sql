DROP DATABASE IF EXISTS passport_demo;
CREATE DATABASE passport_demo;

USE passport_demo;

SELECT * FROM Users;
SELECT * FROM Pets;

-- INSERT INTO Pets(name, age, status, pet_type, sex, chip, collartag, size, color, hair, breed, location, special, photolink)
